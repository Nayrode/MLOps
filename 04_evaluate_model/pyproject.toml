[project]
name = "evaluate-model"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "ipykernel",
    "joblib",
    "matplotlib",
    "mlflow",
    "pandas",
    "python-dotenv",
    "scikit-learn",
    "xgboost>=3.2.0",
]
