[project]
name = "hyperparameter-tuning"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "ipykernel",
    "mlflow",
    "optuna",
    "pandas",
    "python-dotenv",
    "scikit-learn",
]
