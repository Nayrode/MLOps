[project]
name = "model-monitoring"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "evidently",
    "ipykernel",
    "joblib",
    "pandas",
    "python-dotenv",
    "scikit-learn",
]
